version: '2'

services:

  spigot:
    container_name: spigot
    image: jsonxr/spigot
    restart: always
    ports:
      - '25565:25565'
      - '8123:8123'
    working_dir: /minecraft
    volumes:
      - ./minecraft:/minecraft
    command: /minecraft/start.sh
